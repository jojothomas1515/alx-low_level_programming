#include "main.h"

/**
 * print_times_table - times table
 * @num: the table size
 *
 * Description: the multiplication table
 * Return: 0
 */

void print_times_table(int num)
{
	int m = 0, n;

	while (m < (num + 1))
	{
		n = 0;
		while (n < (num + 1))
		{
			int res = n * m;

			if (res < 100)
			{
				int fd = res / 10;
				int ld = res % 10;

				if (fd == 0)
				{
					if (n != 0)
					{
						_putchar(' ');
						_putchar(' ');
						_putchar(' ');
					}
					_putchar(ld + 48);
					if ( n != num)
					{
						_putchar(',');
					}
				}
				else
				{
				}{{{{ds