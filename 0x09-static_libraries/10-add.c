#include "main.h"

/**
 * add - this function add two number
 * @m: first number
 * @n: second number
 *
 * Description: function that add any two number and add the result
 * Return: return the sum of parameters @m and @n
 */

int add(int m, int n)
{
	return (m + n);
}
