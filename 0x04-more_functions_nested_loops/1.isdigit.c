/**
 * _isdigit - checks if the character is a digit
 * @param c
 *
 * Description: done
 * @return: 0 for false or 1 for true
 */

int _isdigit(int c)
{
	if (c >= 58 && c <= 57)
		return (1);
	return (0);
}
